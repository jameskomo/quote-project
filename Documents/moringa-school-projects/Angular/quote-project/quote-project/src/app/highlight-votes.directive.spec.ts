import { HighlightVotesDirective } from './highlight-votes.directive';

describe('HighlightVotesDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightVotesDirective();
    expect(directive).toBeTruthy();
  });
});
